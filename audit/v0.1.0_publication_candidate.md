# QuantForge AI v0.1.0 Publication-Candidate Audit

Validated source HEAD: `84deaf8640df331888f3b0da61c8b4279da55211`  
Authorized audited baseline: `9888f6f64dfb814a9cc07bcdc8d2b4c366ebbe7e`  
Original Phase 1 commit: `2e9da09f4470a1c50e756a06ab935d90f1beaa7a`  
Version: `0.1.0`  
Decision: **QUANTFORGE AI v0.1.0 PUBLICATION CANDIDATE READY — USER AUTHORIZATION REQUIRED**

The validated source HEAD was the clean final local HEAD when the complete release transaction ran.
This report is stored by a subsequent normal audit-only commit because a Git commit cannot embed its
own SHA. The report-storage commit is resolved from Git and reported at handoff; it changes no
package, workflow, lock, or release-artifact input.

## Scope and source identity

Git independently returned the authorized 40-character baseline and exact message `Harden Phase 1
after independent audit`. The baseline and original Phase 1 commit are ancestors of the validated
HEAD. The QuantForge tree and index were clean, no remote was configured, and no history was amended
or rewritten. No tag, push, GitHub repository, release, or upload was created.

Both Phase 1 audit reports remain present and agree on the passed, Phase-2-ready verdict. The
machine-readable repository gate also verified that findings H-01 through H-05 and M-01 through M-04
are recorded as repaired with regression evidence.

## Version and publication infrastructure

`src/quantforge/_version.py` is authoritative. Hatch metadata, the installed runtime, CLI
`--version`, changelog, `CITATION.cff`, release notes, CycloneDX metadata, and generated validation
reports all resolve to `0.1.0`.

The repository now contains professional README, changelog, citation, conduct, support, security,
contribution, release-policy, and release-note material; deterministic release and package-inspection
tools; hash-complete development locking; Dependabot; and five workflows for CI, Security, CodeQL,
Reproducibility, and exact-ref Release Candidate validation.

The remote matrix is Ubuntu 24.04/Python 3.12 for the complete gate; Ubuntu 24.04/Python 3.13 and
3.14, macOS 14/Python 3.12, and Windows Server 2022/Python 3.12 for compatibility. Only the recorded
local macOS arm64/Python 3.12.13 result is claimed here. Every remote platform remains pending until
the exact final publication SHA passes on GitHub.

All 15 third-party action references resolve to five immutable pins:

- `actions/checkout` v6.0.2 — `de0fac2e4500dabe0009e67214ff5f5447ce83dd`
- `actions/setup-python` v6.2.0 — `a309ff8b426b58ec0e2a45f0f869d46889d02405`
- `actions/upload-artifact` v7.0.1 — `043fb46d1a93c77aae656e7c1c64a875d1fc6a0a`
- `github/codeql-action/init` v4.37.0 — `99df26d4f13ea111d4ec1a7dddef6063f76b97e9`
- `github/codeql-action/analyze` v4.37.0 — `99df26d4f13ea111d4ec1a7dddef6063f76b97e9`

Workflows use explicit least privilege, disabled persisted checkout credentials, lock-bound caches,
concurrency controls, and no mutable Docker tag. Static YAML and repository policy validation passed.
CodeQL is defined but, correctly, is not claimed to have run locally.

## Exact validation results

The final transaction used a new CPython 3.12.13 environment installed from the 60-distribution
development lock with `--require-hashes`, followed by a non-editable project install without
dependency resolution or build isolation.

- Formatting and Ruff: passed.
- Strict mypy: passed across 37 source files.
- Full branch-aware suite: **289 passed**, 0 skipped.
- Reverse-file-order suite: **289 passed**, 0 skipped.
- Malicious-input selection: **25 passed**, 264 deselected.
- Overall coverage: **92.40%**, including 424 of 502 branches; minimum 90%.
- Governance-critical aggregate coverage: **92%**; minimum 90%.
- Runtime and development dependency audits: no known vulnerabilities found.
- Current-file and reachable-history secret scan: passed.
- CFF schema validation and 20 local links: passed.
- Package inventory, archive safety, wheel RECORD integrity, license, and notice: passed.

The first complete attempt exposed two defects in the new installed-wheel gate: copied POSIX
launchers broke this macOS interpreter's shared-library resolution, and the smoke expected a
nonexistent `CLOSED` state. The root repair uses symlinked POSIX venv launchers, preserves copied
Windows launchers, and requires the real `CHAIR_EXPLANATION` terminal state. Two regression tests
were added in commit `84deaf8640df331888f3b0da61c8b4279da55211`; a second fresh-environment transaction passed.

## Packages, SBOM, and checksums

- Wheel: `quantforge_ai-0.1.0-py3-none-any.whl` — SHA-256
  `80b6b24c59829b2b265c60375b4cde95d2ceef93ede5546ef74404bf046de71a`; 38 members.
- Source distribution: `quantforge_ai-0.1.0.tar.gz` — SHA-256
  `95ee1fe39e08855961281b672537959e44ab7bd1b5ad0be029d15c83d3665e18`; 95 members.
- Two isolated builds of both distributions were byte-identical.
- The wheel was installed outside the source tree from the five-distribution runtime lock. `pip
  check`, runtime/CLI version, the fragile demo, case validation, and 12-event semantic audit replay
  passed.
- CycloneDX 1.6 SBOM: `quantforge-ai-v0.1.0-sbom.cdx.json` — SHA-256
  `a5e94fb5dce4b2c430f4c7bc792545bbca06bea8eed2d25cb35ea21f68ef1e09`; serial
  `urn:uuid:abc0b703-b514-5d87-8424-7042a4f0971e`. It binds the source commit, wheel digest, five
  runtime components, six dependency entries, and `cyclonedx-python-lib==9.1.0`.
- `SHA256SUMS` — SHA-256
  `1e879670eaeb50d9a0cd5c1f7513fcb1ce6b38181ebec4cceafe000c8a7f385f`. Independent verification
  passed for all six listed release assets.

## Determinism and security

All `provisional`, `fragile`, and `inconclusive` scenarios ran twice. Fifteen governed artifact pairs
were byte-identical; canonical hashes, semantic audit replay, and verdict stability passed. Exact
per-artifact SHA-256 evidence is retained in the machine report.

Both exact lock graphs were queried against current advisory data and returned no known
vulnerabilities. Secret scanning covered current files and reachable Git history. Malicious JSON,
audit, evidence, and path regressions passed. Packages contain only controlled paths and validated
metadata. No credential was used.

## Protected engine proof

The read-only `cpp-event-driven-backtester` boundary matched before and after:

- HEAD `9266f317573e90452ca88c7d30196d8b6d6f21a3`, branch `main`;
- tracked diff empty, staged diff empty, untracked inventory empty;
- 16,301 ignored entries, inventory SHA-256
  `9f99224e3a34dfd512442f0e0931ca201a896f66265aefa91d2992a09f4e092b`;
- annotated `v1.0.0` tag object `20ac53c5e4b61ae7b431d5bb263f246e35f8d2a2`;
- peeled target `2f86b71dbc9f29dbda861942d8afbb10c04b6625`.

No engine state changed.

## Remaining limitations and authorization boundary

This is an audited offline Phase 1 governance foundation, not the complete platform or a production
deployment. It has no real OpenAI execution, live model provider, C++ integration, market data,
retrieval, database, web UI, broker connectivity, orders, live trading, autonomous investment
authority, profitability claim, or investment advice. The local audit chain is not externally signed
or anchored. Remote CI has not run and no nonlocal platform is claimed to pass.

The repository is ready for GitHub source publication, but this report does not authorize it. The
next required authorization is explicit permission to create the GitHub repository, configure the
first remote, and push the exact final report commit. Exact-SHA remote CI must then pass before a
separate authorization may create the annotated tag and public release.

**QUANTFORGE AI v0.1.0 PUBLICATION CANDIDATE READY — USER AUTHORIZATION REQUIRED**
